
[package]
name = "rf_copilot"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2.0.0", features = ["api-all", "shell-open"] }
tauri-plugin-shell = { version = "2", features = ["open"] }
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
env_logger = "0.11"

[dev-dependencies]
